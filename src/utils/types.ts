/**
 * 定义项目中使用的类型
 * 
 * 注意: 此文件保留用于向后兼容
 * 新代码应从 '@/core/types' 导入类型
 */

// 从核心模块重新导出所有类型
export type {
  Model,
  ModelType,
  GroundType,
  TransformMode,
  ThreeInstance,
  ColorMapType,
  ModelLoadCallbacks,
  PressureStats,
  PressureVisualization,
  PressureDataOptions
} from '../core/types'