# FTMI Viewer 升级开发技术文档

（Web 端三维工程模型查看器）

## 0. 项目背景与目标

FTMI Viewer 面向 **工程 BIM / 机械三维模型** 浏览场景，需要具备：

* 全 Web 运行，无插件、无本地安装成本
* 支持多种工业模型格式（STEP / IGES / STL / FBX / GLB / OBJ）
* 可视化分析（测量、剖切、爆炸图、层级管理）
* 高保真渲染（HDRI + PBR + 后处理）
* 可扩展的数据缓存与外部拉取机制

本轮升级目标：

1. 构建统一的数据接入层，实现跨格式模型解析
2. 引入工程测量、剖切、爆炸分解等工业软件功能
3. 提升渲染表现，实现工业级光影与后处理
4. 支持远程模型加载、缓存持久化、本地二次快速打开
5. 明确渲染、交互、数据的三层架构，便于后期扩展

---

## 1. 数据兼容性与数据接入层 (Data Access Layer)

### 1.1 多格式解析路线

Web 环境无法直接处理 STEP、IGES 等 CAD NURBS 曲面，需要转换为三角网格。
可用两类方案：

#### **方案 A：WASM 转换**

* 使用 `occt-import-js` 或 `xeokit-gltf-convert`
* 在浏览器 WebAssembly 运行 OCCT 内核
* 把 STEP/IGES → MeshBuffer → Three.js BufferGeometry

优势：

* 转换本地完成，无需服务端 CAD 内核
* 适配离线、隐私、浏览器沙盒场景

难点：

* WebAssembly 线程调度
* 转换时 CPU 占用可能峰值高

优化：

* Web Worker 放置转换逻辑
* 分块网格生成，结合 `postMessage`

#### **方案 B：原生 Loader 补充**

* `STLLoader`：适配轻量三维零件
* `OBJLoader`：贴图模型，可用于场景
* `FBXLoader`：保留层级结构与材质（适合工程场景）

所有 loader 统一生成：

```
StandardMesh = { geometry, material, metadata }
```

并进入统一的 **BufferGeometry 处理流水线**。

---

### 1.2 标准化 Mesh 预处理（Pipeline）

所有 Mesh 在入场前执行：

1. 法线重计算：`geometry.computeVertexNormals()`
2. 单面材质替换为 `DoubleSide`（可用于剖切）
3. Tangent/Bitangent 重构，用于法线贴图
4. 若网格数据较大 → `three-mesh-bvh` 建索引（提升射线拾取）

问题 & 突破：

* 大模型 BVH 构建慢
  → 使用 Lazy 构建策略，仅在测量功能首次触发时初始化

---

### 1.3 本地持久化 (IndexedDB + 缓存策略)

#### 技术栈

* 使用 `idb` 库封装 IndexedDB
* 缓存 BLOB、二进制 GLB、纹理、BVH 数据

#### LRU 回收策略

存储上限设为 1~2GB，当达到上限：

* 记录访问时间戳
* 删除最老缓存
* 保留变换过的多边形文件（加载成本高）

难点突破：

* IndexedDB 存数百 MB BLOB 会卡顿
  解决方式：
* 使用 `await idb.put` 结合事务
* 避免单记录过大，分块存储

---

### 1.4 外部 URL 传参加载

示例：

```
https://viewer.ftmi.com/?model=https://cdn.xxx.com/engine.glb
```

解析流程：

* 解析 Query 参数
* 判断是否缓存存在
* 无缓存 → fetch 下载 → 存入 IndexedDB → 解码加载

#### CORS 难点与解决

远程 Blob 拉取可能失败：

* 推荐服务端配置：

```
Access-Control-Allow-Origin: *
Access-Control-Allow-Headers: Range
```

若第三方不支持 CORS：

* 使用代理签名请求（Cloudflare Worker）

---

## 2. 工程空间分析与高级交互 (Engineering Tools)

模型展示的核心价值不是“看见”，是**分析**。

---

### 2.1 精度测量系统

#### 技术方案

* 使用 `three-mesh-bvh` 提升 `raycast` 速度
* 在点击点建立“Pick Point”
* 多点坐标计算几何参数

#### 距离计算

两点距离：

[
d = | p_1 - p_2 |
]

支持：

* 点到点
* 点到边
* 点到三角面

#### 角度计算

向量夹角：

[
\theta = \cos^{-1}\left(\frac{u \cdot v}{|u||v|}\right)
]

难点：

* 三角网格粗糙导致面法线波动
  解决：
* 对最近点三角集合做平均法线

---

### 2.2 剖切系统（Clipping）

核心技术：

* `renderer.localClippingEnabled = true`
* 定义全局 clippingPlanes 数组
* 为材质设置 `clipping: true`、`side: DoubleSide`

交互逻辑：

* 使用 `TransformControls` 控制 Plane
* Plane 更新 → Shader 重新裁切
* 控制器可以绑定坐标轴锁定

难点突破：

* 多 Plane 同时作用造成渲染抖动
  解决：
* 限制同时开启 Plane 个数 ≤ 3

---

### 2.3 爆炸视图算法

实现路线：

1. 遍历 SceneGraph
2. 根据 boundingBox 计算中心
3. 确定偏移方向（全局向外 or 父系中心）
4. 用 Tween 补间 position

支持：

* 线性、EaseInOut
* 动画反向（还原组装）
* 保留原坐标缓存

难点：

* 大场景 500+ Mesh 动画同步卡顿
  优化策略：
* 每帧仅更新部分节点（批量 100）
* requestIdleCallback 用于非关键时

---

### 2.4 层级管理 UI

SceneGraph → Tree 结构：

```
uuid
name
visible / opacity
children
```

UI 操作同步：

* `mesh.visible = false`
* `material.opacity = 0.3; transparent=true`
* 通过 UUID 精确定位 Mesh 节点

难点：

* 材质复用导致多 Mesh 同步变化
  解决：
* 克隆材质再继承参数（避免污染）

---

## 3. 环境光照与渲染引擎 (Rendering Engine)

### 3.1 PBR 光照增强

HDR 处理流程：

* `RGBELoader` 加载 HDR
* `PMREMGenerator` 转换为 prefiltered radiance textures
* 绑定到 scene.environment

提供三套配置：

* Studio（高光、低粗糙）
* Outdoor（高亮度、蓝光）
* Industrial（暖光偏钢铁材质）

难点：

* 移动端 PMREM 超内存
  解决：
* 低分 HDR + Lazy apply

---

### 3.2 后处理管线

使用 `EffectComposer`，顺序：

1. RenderPass
2. **SAOPass** → 空间遮挡
3. **UnrealBloomPass** → 高光扩散
4. **SMAAPass** → 抗锯齿

难点：

* SAO 计算极耗 GPU
  解决：
* 在模型静止时启用 SAO
* 动画操作关闭 AO

---

### 3.3 实时材质编辑器

对 `MeshStandardMaterial` 做参数暴露：

| 参数          | 效果   |
| ----------- | ---- |
| metalness   | 金属属性 |
| roughness   | 粗糙度  |
| emissive    | 自发光  |
| normalScale | 法线强度 |

UI 双向绑定：

* UI 数值变化 → 材质直接反映
* 材质更新时 Shader Hot Reload

---

## 4. 性能优化策略

### 四大方向：

#### **1. 模型加载性能**

* 大模型使用 Drac压缩
* Lazy BVH 构建
* GLB 纹理统一贴图压缩

#### **2. 渲染优化**

* 视锥裁剪
* `MeshBatch` 合并对象
* `InstancedMesh`

#### **3. Worker 加速**

* CAD 转换进 Web Worker
* PostMessage 二进制传输

#### **4. 缓存复用**

* BVH、BufferGeometry、纹理缓存

---

## 5. 安全与浏览器资源管理

* 防止浏览器 4GB Memory crash
* 限制 Worker 同时运行数量
* IndexedDB 上限检测
* 自动回收大文件

---

## 6. 开发架构设计

分三层：

### **Data Layer**

* format loaders
* WASM convertor
* IndexedDB cache

### **Render Layer**

* Three.js Renderer
* Post-processing
* HDR/PBR Material

### **Interaction Layer**

* Measurement
* Cutting
* Explode
* Hierarchy UI

每层可插拔、解耦。

---

## 7. 关键技术难点与解决方案总结

| 难点                    | 解决路径            |
| --------------------- | --------------- |
| STEP/IGES 无法直接 Web 渲染 | WASM + OCCT 三角化 |
| 大模型 raycast 卡顿        | three-mesh-bvh  |
| IndexedDB 大 Blob 性能差  | 分块 + LRU        |
| SAO/后处理 GPU 压力大       | 动静态启停策略         |
| 材质共享污染状态              | 克隆材质            |
| 交互 Plane 抖动           | 限制 Plane 数量     |
| 爆炸动画卡顿                | 批量更新 + Idle 回调  |

---

## 8. 测试矩阵

包含：

* Chrome / Edge / Safari
* PC / iPad / Android
* 大模型（>300MB）
* 多 Plane 剖切
* 1000+ Mesh 层级
* 远程 CORS

---

## 9. 交付与未来扩展路线

未来扩展可考虑：

* WebGPU renderer
* GPGPU 加速 Mesh Simplification
* 断面面积/体积分析
* 贴图烘焙
* 在线协作与标注

---
