## 一、总体策略

- **不推翻现有结构**：保留 `SceneManager / ModelLoader / ModelProcessor / threeLoader.loadModel / ModelViewer` 这套主线。
- **按三层架构推进**：
  - Data Layer：多格式 + STEP/IGES + 预处理 + 缓存 + 远程 URL。
  - Render Layer：HDR/PBR + 后处理。
  - Interaction Layer：测量 / 剖切 / 爆炸 / 层级管理。
- 每一层内部再拆为若干“小里程碑”，保证随时可回滚。

---

## 二、阶段 1：基础重构与 Data Layer 框架（优先做）

### 1.1 整理模型类型 & 加载入口

**目标**：让所有模型加载都走统一的 Data Layer 入口，为后续 STEP/IGES、缓存、远程加载打基础。

- **任务**
  - 在 `core/types` 中确认/扩展 `Model`：
    - `type: 'glb' | 'gltf' | 'fbx' | 'obj' | 'stl' | 'step' | 'iges' | ...`
    - `file: string`（可为本地路径或远程 URL / blob URL）
    - 可加 `source: 'local' | 'remote' | 'cache'` 标记（可选）。
  - 在 `src/utils/threeLoader.ts` 中保持 `loadModel` 作为**统一入口**，后续所有 Data Layer 都挂在它内部。

> 完成标志：依旧能加载现有 glb/obj/fbx/stl，且类型扩展不会破坏现有逻辑。

---

### 1.2 引入多格式加载 Pipeline

**目标**：建立统一 Mesh Pipeline（法线、双面、BVH）。

- **任务**
  - 新增 `src/core/pipeline/MeshPipeline.ts`（名字可自定），集中处理：
    - `normalizeMesh(object: Object3D)`：
      - 统一材质为 `MeshStandardMaterial` / `DoubleSide`。
      - 重计算法线。
    - 与 BVH 结合（只挂接口，先不实现 BVH）。
  - 在 `ModelLoader` 中，每种格式加载完成后都调用 `MeshPipeline.normalizeMesh`，而不是散落在各处。

> 完成标志：现有格式加载结果一致，但代码集中、易扩展。

---

### 1.3 远程 URL 模型加载 + ZIP 支持

**目标**：打通第三方工作流嵌入：`?src=...` 直接加载远程 glb/zip。

- **任务**
  - 新增 `src/utils/remoteModel.ts`：
    - `buildRemoteModelFromQuery(query): Promise<Model>`
      - 解析 `src/type/zipEntry/ground/name`。
      - 如为 zip：使用 `jszip` 解压指定 entry → 生成 `blob:` URL。
  - 在 viewer 路由页面（例如 `pages/ViewerPage.vue` 或当前入口）：
    - 若存在 `src` 参数，则调用 `buildRemoteModelFromQuery`，把结果作为 `ModelViewer` 的 `model`。
  - CORS 策略：
    - 前端只做基本校验和错误提示。
    - 在文档中约定：推荐使用你的代理 / CDN 配置。

> 完成标志：能通过 URL 参数加载远程 glb；zip 可稍后补上。

---

### 1.4 IndexedDB 缓存（基础版）

**目标**：首次拉远程模型写入缓存，下次打开走本地。

- **任务**
  - 使用 `idb` 封装：
    - 新建 `src/core/cache/ModelCache.ts`：
      - `get(modelKey): Promise<ArrayBuffer | null>`
      - `set(modelKey, buffer: ArrayBuffer): Promise<void>`
      - `touch(modelKey)` 用于 LRU。
  - 在 `remoteModel.ts` 内：
    - 下载远程文件前先 `cache.get(key)`。
    - 若无缓存：`fetch` 完写入缓存后再给 Loader。
  - 先不实现 LRU 清理，只记录 `lastAccess` 字段，后续阶段完善。

> 完成标志：重复打开同一个远程 URL，第二次明显更快（实际走缓存）。

---

## 三、阶段 2：STEP / IGES + WASM 集成

### 2.1 选型 & 封装 StepIgesLoader

**目标**：把 CAD → Mesh 的复杂度封装在一个文件里。

- **任务**
  - 选择方案（根据你能接入的库）：
    - 首选：`occt-import-js`（纯前端 wasm）。
    - 备选：自己服务端转换为 glb（前端只当 glb）。
  - 新增 `src/core/loaders/StepIgesLoader.ts`：
    - `init()`：懒加载 wasm & 初始化，仅执行一次。
    - `loadStep(url: string): Promise<Object3D>`
    - `loadIges(url: string): Promise<Object3D>`
  - 内部流程：
    - `fetch(url) → arrayBuffer → wasm.parse(buffer)`。
    - 遍历 B-Rep → 三角化 → `BufferGeometry` → `Mesh` / `Group`。

### 2.2 与 ModelLoader 整合

- 在 `ModelLoader`：
  - `type` 新增 `'step' | 'iges'` 分支，调用 `StepIgesLoader`。
  - 加上 `callbacks.loading` 进度更新（如：初始化 10%，解析 20~90%，mesh 90~100%）。

> 完成标志：能加载测试 STEP/IGES 文件，并进入与其他格式相同的渲染与交互流程。

---

## 四、阶段 3：Interaction Layer（核心功能）

### 3.1 测量系统（Distance / Angle）

**目标**：实现工程测量，是整个 Interaction 的基础。

- **任务**
  - 引入 `three-mesh-bvh`：
    - 在 MeshPipeline 中，支持对较大 mesh 构建 BVH（首次用到测量时才初始化）。
  - 新增 `src/core/controllers/MeasureController.ts`：
    - 包含：
      - 射线拾取封装：`pick(screenX, screenY): PickResult`
      - 计算点到点距离 / 角度。
      - 内部使用 BVH 加速 `raycast`。
  - 组件：
    - 新建 `MeasurementPanel.vue`：
      - 点选两点展示距离。
      - 显示坐标与单位（mm/m，可后期加）。

> 完成标志：基础距离测量稳定可用，大模型下也可接受。

---

### 3.2 剖切系统（Clipping Planes）

**目标**：实现 X/Y/Z 动态剖切，对齐文档中的 Plane 管理。

- **任务**
  - 新增 `src/core/controllers/ClippingController.ts`：
    - 管理 `THREE.Plane` + 绑定 `renderer.clippingPlanes`。
    - 提供：
      - `setAxisPlane(axis, t)`：t 映射到模型 bbox。
      - `toggleAxis(axis, enabled)`
      - `reset()`
  - 在 `loadModel` 中：
    - 模型加载完后创建 `ClippingController`，并挂到 `ThreeInstance` 上。
    - 在 `dispose()` 里销毁。
  - Vue UI：
    - `ViewerToolbar` 增加“剖切”按钮 → 打开 `ClippingPanel.vue`：三个滑块 + 勾选。

> 完成标志：可通过 UI 对任意模型做 X/Y/Z 剖切，性能稳定。

---

### 3.3 爆炸视图（Exploded View）

**目标**：实现一键拆解 + 滑杆控制爆炸程度。

- **任务**
  - 新增 `src/core/controllers/ExplodedViewController.ts`：
    - `compute(group)`：记录所有零件的质心、方向向量与初始位置。
    - `setFactor(t)`：根据 t 线性偏移。
    - `reset()`：还原位置。
  - 在 `loadModel`：
    - 模型处理完（居中/缩放/贴地）后，初始化 `ExplodedViewController`。
  - Vue UI：
    - 增加“爆炸视图”按钮 + Slider（0~1）。

> 完成标志：中等大小装配模型下爆炸/合拢流畅，且能恢复原位。

---

### 3.4 层级管理（Scene Graph）

**目标**：构建完整 Scene Graph + 显隐/锁定/透明。

- **任务**
  - 新增 `src/core/scene/SceneGraphBuilder.ts`：
    - 把 `scene` 或 `group` 转为树 `SceneNode[]`。
  - ThreeInstance 扩展：
    - `getSceneGraph(): SceneNode[]`
    - `applyNodeVisibility(uuid, visible)`
    - `applyNodeOpacity(uuid, opacity)`
    - `applyNodeLock(uuid, locked)`
  - Vue store（Pinia 或 composable）：
    - 保存树结构 + 选中状态。
  - `SceneGraphPanel.vue`：
    - 基于 Element Plus Tree。
    - 左侧树，右侧节点控制：显隐/锁定/透明度。

> 完成标志：能通过 UI 控制单个零件/子树的显隐和透明度，锁定状态能阻止被选中/操作。

---

## 五、阶段 4：Render Layer（HDR / PBR / 后处理）

### 4.1 HDR 环境与 PBR

- **任务**
  - 使用 `RGBELoader + PMREMGenerator`：
    - 新建 `src/core/environment/EnvironmentManager.ts`：
      - 预加载多套 HDR。
      - 提供切换 API：`setEnv('studio' | 'outdoor' | 'industrial')`。
  - 在 `SceneManager`/`loadModel` 中注入环境光逻辑。

### 4.2 后处理管线

- **任务**
  - 使用 `EffectComposer`：
    - `RenderPass` + `SAOPass` + `UnrealBloomPass` + `SMAAPass`。
  - 在 `SceneManager` 引入可选的 `composer` 渲染路径：
    - 默认关闭重型效果（SAO），交互暂停时自动开启（按文档策略）。

### 4.3 实时材质编辑器

- **任务**
  - 设计 `MaterialEditorController`：对 MeshStandardMaterial 的金属度/粗糙度等属性访问接口。
  - UI：`MaterialEditorPanel.vue`，可针对选中 Mesh 调节属性。

> 完成标志：至少一套 HDR + 基础后处理跑通，材质编辑可对部分 Mesh 生效。

---

## 六、阶段 5：性能 & 安全策略落地

### 5.1 Worker 化

- **任务**
  - 将 STEP/IGES WASM 转换与 BVH 构建迁入 Web Worker：
    - `workers/cadWorker.ts` / `workers/bvhWorker.ts`。
    - 主线程与 Worker 通过 `postMessage` 传输 ArrayBuffer / Float32Array。
  - 遵循《升级文档》中的“限制 Worker 数量”。

### 5.2 LRU 缓存与资源上限

- **任务**
  - 在 `ModelCache` 中增加：
    - `maxSize`（总容量）。
    - LRU 算法：超出时按 `lastAccess` 删除。
  - 内存安全：
    - 大模型加载前检测浏览器内存（近似，基于经验与配置），超限提示用户。

> 完成标志：在高频切换大模型场景下，内存和磁盘空间都可控。

---

## 七、阶段 6：测试矩阵 & 发布

- 根据《升级文档》的测试矩阵：
  - 设计自动化/手动测试 checklist。
  - 重点验证：
    - 浏览器（Chrome/Edge/Safari）
    - 设备（PC/Pad/Android）
    - 大模型 + 多 Plane 剖切 + 1000+ Mesh 层级
    - 远程 CORS / 代理场景

---
